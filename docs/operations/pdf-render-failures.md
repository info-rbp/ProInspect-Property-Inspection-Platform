# PDF render failure recovery

Confirm the approved report version, template version, asset generations, final bucket, Chromium executable and service identity. Re-run the same deterministic render input: existing matching objects are reused after hash verification. A path with a different hash is an immutable collision and requires incident escalation, not overwrite. Finalisation remains blocked until the package record and verified manifest exist.
