# Import recovery

Imports retain original source generations, extraction/mapping versions, warnings and candidate decisions. Retry failed extraction with the same source document; confirm that the resulting candidates still point to their source page/image. A human must accept or amend at least one candidate before confirmation. Never silently replace canonical report facts or discard rejected candidates.
