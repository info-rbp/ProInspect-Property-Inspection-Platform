# Delivery failure recovery

Verify recipient authority, immutable package assets, expiry, sender configuration and provider response without copying tokens or personal content into logs. Retry through the named delivery command with the same package; revoke any suspected token immediately. Provider callbacks must deduplicate by provider event ID. Repeated bounces require agency review rather than automatic alternate-recipient delivery.
